- [ ] `log_retention_days` 与 `log_cleanup_period` 已加入默认设置。
- [ ] `logger.py` 重写后仍保持单例、多 Handler、标准级别映射，且新增崩溃安全写入。
- [ ] `log_file_handler.py` 按日期命名日志文件，并按设置保留天数清理旧日志。
- [ ] 启动时日志模块读取设置并完成清理，记录清理结果。
- [ ] `crash_handler.py` 已安装全局 `sys.excepthook`，可捕获 `main.py` 自身异常。
- [ ] 崩溃日志包含异常类型、消息、完整 traceback，并存入 `runtime/logs/crashes/`。
- [ ] 崩溃时自动启动中文崩溃提示工具，并传入崩溃日志路径。
- [ ] 崩溃提示窗口显示崩溃原因、文件/行号、可展开堆栈、操作按钮。
- [ ] 设置界面“日志设置”分组增加了“日志保留时间”和“自动清理周期”。
- [ ] 设置界面与 `log_retention_days`、`log_cleanup_period` 双向绑定。
- [ ] 版本号按 0.01 步长递增，更新日志已补充。
- [ ] 所有新增/修改文件通过 `python -m py_compile` 编译检查。
- [ ] 程序可正常启动，崩溃提示工具可正常弹出。
