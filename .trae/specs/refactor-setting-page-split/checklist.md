- [x] `core/settings/history_cleaner.py` 存在并实现 `HistoryCleaner.clean_all()` 静态方法
- [x] `core/settings/ui_refresher.py` 存在并实现 `SettingsRefresher.refresh_from(page)` 静态方法
- [x] `core/settings/auto_save_connector.py` 存在并实现 `AutoSaveConnector.connect(page, callback)` 静态方法
- [x] `core/settings/__init__.py` 导出 `HistoryCleaner`、`SettingsRefresher`、`AutoSaveConnector`
- [x] `gui/pages/setting_page.py` 的 `_clear_history` 委托给 `HistoryCleaner.clean_all()`，保留确认对话框
- [x] `gui/pages/setting_page.py` 的 `_refresh_ui` 委托给 `SettingsRefresher.refresh_from(self)`
- [x] `gui/pages/setting_page.py` 的 `_connect_auto_save_signals` 委托给 `AutoSaveConnector.connect`
- [x] `gui/pages/setting_page.py` 保留所有 UI 创建、样式、GUI 交互（文件对话框、颜色选择、Token 高亮动画）代码
- [x] `gui/pages/setting_page.py` 保留已有的薄包装方法（TokenManager/SettingsSaver/SettingsExporter/DomainManager 委托）
- [x] 所有修改文件通过 `python -m py_compile` 编译
- [x] `setting_page.py` 行数显著减少（从 1748 行减少至 1545 行）
- [x] SettingPage 对外行为不变（settings_saved 信号、load_settings/load_access_token 静态方法、highlight_token_input 方法仍可用）
